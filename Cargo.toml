[package]
name = "pretty-colors"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
minifb = "0.28"
rand = "0.10"
rayon = "1.10"
wide = "1.5.0"

[profile.release]
lto = true
