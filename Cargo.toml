[package]
name = "pretty-colors"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6", features = ["derive"] }
minifb = "0.28"
noise = "0.9"
rand = "0.10"

[profile.release]
lto = true
